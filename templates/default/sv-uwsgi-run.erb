#!/bin/sh
set -e

RELESED_FILE="<%= @released_file %>"
RELESED=$(cat $RELESED_FILE)

. <%= @virtualenv %>/bin/activate

exec chpst -u <%= @user %>:<%= @group %> uwsgi -x <%= @config_file %> --procname <%= @name %>-$RELESED