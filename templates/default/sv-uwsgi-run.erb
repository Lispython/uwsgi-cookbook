#!/bin/sh
set -e

RELEASED_FILE="<%= @options[:released_file] %>"
RELEASED=$(cat $RELESED_FILE)

. <%= @options[:virtualenv] %>/bin/activate

exec chpst /usr/local/bin/uwsgi -x <%= @options[:config_file] %> --procname <%= @options[:name] %>-$RELESED