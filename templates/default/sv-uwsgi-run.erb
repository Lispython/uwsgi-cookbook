#!/bin/sh
set -e

RELEASED_FILE="<%= @released_file %>"
RELEASED=$(cat $RELESED_FILE)

. <%= @virtualenv %>/bin/activate

exec chpst /usr/local/bin/uwsgi -x <%= @config_file %> --procname <%= @name %>-$RELESED